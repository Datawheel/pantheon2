import React, { Component, PropTypes } from 'react';

class Viz extends Component {

  constructor(props) {
    super(props);
  }

  render() {
    const {data} = this.props;

    return (
      <div>
        Viz here...
      </div>
    );
  }
};

export default Viz;
